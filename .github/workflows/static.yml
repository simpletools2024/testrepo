name: Inject AppJS and Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

# Rechten voor GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inject script tag in HTML files
        run: |
          # Bepaal de naam van de repository voor het juiste pad
          REPO_NAME="${{ github.event.repository.name }}"
          SCRIPT_TAG="<script src='/$REPO_NAME/app.js'></script>"
          
          echo "Injecteren van: $SCRIPT_TAG"
          
          # Zoek alle .html bestanden en plak de tag bovenaan de inhoud
          find . -type f -name "*.html" -not -path "./.git/*" -exec sh -c "echo '$SCRIPT_TAG' | cat - \"\$1\" > temp && mv temp \"\$1\"" _ {} \;
          
          echo "Injectie voltooid."

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
